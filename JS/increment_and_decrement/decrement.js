/*Write a sum function that returns the sum of three variable values,
where each is decreased by one. Use variables a, b and c as function parameters.
*/

function sum(a, b, c) {
    return (--a + --b + --c);
}