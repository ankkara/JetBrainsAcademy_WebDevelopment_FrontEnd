// Declare a function named multiplication, which takes any two numbers as parameters and returns the result of their multiplication.

let multiplication = (a, b) => { return a * b; };

console.log (multiplication(2, 6));